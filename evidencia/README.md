# evidencia/

| Archivo | Estado |
|---|---|
| `resultados.csv` | ✅ generado por `scripts/run_benchmark.py` |
| `benchmark_summary.md` | ✅ generado por `scripts/run_benchmark.py` |
| `dashboard.png` | ⚠️ **placeholder** — es una imagen genérica generada solo para que `docs/PE_U3_Informe.tex` compile. Reemplazar por una captura real de `http://localhost:8080` (o el puerto que uses) mostrando los 3 nodos `LIVE` **antes de entregar**. Requiere criterio 1.2. |
| `video_tolerancia.mp4` | ⬜ **pendiente** — grabación de pantalla (2–5 min) de la prueba de tolerancia a fallos. Requiere criterio 2.4. Guion de comandos en el [README](../README.md#prueba-de-tolerancia-a-fallos). |

`dashboard.png` debe reemplazarse manteniendo el mismo nombre de archivo, así
`docs/PE_U3_Informe.tex` sigue compilando sin cambios. `video_tolerancia.mp4`
es enteramente manual (grabación de pantalla real), no se puede generar por
script.
